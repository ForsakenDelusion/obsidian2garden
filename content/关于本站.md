---
publish: true
title: 关于本站
created: 2026-09-08
modified: 2026-09-08T12:00:00.000Z
tags:
  - 关于
---

# 关于本站

这里是我的数字花园，记录学习、折腾与思考。站点由 [Quartz](https://quartz.jzhao.xyz) 构建，用于把 Obsidian 中的笔记发布成静态博客。

## 站点信息

- **名称**：Delusion's Garden
- **地址**：https://delusion.uno
- **构建**：Quartz 5
- **主题**：letterpress poster / cyanotype（复刻自 jzhao.xyz）

---

# Changelog

## 2026-09-08

- 新增本站「关于」页面，将 changelog 迁移至此统一维护。
- 复刻 jzhao.xyz 的配色方案（letterpress poster / cyanotype）：浅色暖纸张 + 藏青正文 + 朱红强调，深色普鲁士蓝 + 天青 + 朱红强调。
- 调整文章内链接为朱红色下划线（"second ink" 风格），替换默认 pill 背景。
- 重新加入 tag pill 的悬停反馈动画（上浮 + 阴影）。
- 修复 tag pill 无 hover 反馈的问题，加入平滑过渡动画。
- 优化 giscus 评论区主题，使其与 letterpress / cyanotype 配色一致：新建 `quartz/static/custom-theme/` 存放自定义亮/暗主题 CSS（`light.css` / `dark.css`），并通过 giscus 的 `options.themeUrl`（`https://delusion.uno/static/custom-theme`）加载，避免直接改动官方 `quartz/static/giscus/` 源文件（防止以后更新 Quartz 时冲突）。

## 2026-09-05

- 新增并接入自定义 **Obsidian Admonition 插件**（`github:ForsakenDelusion/quartz-obsidian-admonition`），通过 `github:` 引用方式加入 `quartz.config.yaml`。支持 `` `ad-<type>` `` 代码块转 callout、`title` / `collapse` / `icon` / `color` / `metadata` 选项，以及用递减反引号写的**嵌套** admonition（外层 5 → 内层 4 → 内内层 3 反引号）。
- 插件以 TypeScript + tsup 开发，预构建 `dist/` 一并提交（避免 npm 全局 `omit=dev` 导致 Quartz 侧无 tsup 而构建失败），Quartz 检测到预构建 dist 直接加载，无需二次构建。
- 本地端到端验证通过：`npx quartz build` 正确渲染出 note / tip(is-collapsible) / warning / danger 四类 callout，含嵌套结构。

## 2026-08-25

- 从 Aura 主题切换回 Quartz 默认主题，因为 Aura 对 Explorer 标题字号的样式覆盖会导致折叠后内容被截断，而默认主题显示正常。

## 2026-08-24

- (早期内容占位)
